import { Button } from "@/components/ui/button";

const AddtoCart = () => {
  return (
    <div>
      <Button className="mt-6 h-12 px-6 text-xs">Add to cart</Button>
    </div>
  );
};

export default AddtoCart;
